[project]
name = "iron-python-learning"
version = "0.1.0"
description = "Runnable Python learning stages for Java engineers"
readme = "README.md"
authors = [
    { name = "pbxjtumse", email = "pbxjtumse@outlook.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
